# Test Git Flow

This repository tests the git-flow workflow automation.

## How it works

1. Push to `main` branch
2. Workflow triggers and calls `cpdevtools/git-flow` reusable workflow
3. Workflow creates `release/main` branch
4. Draft PR is created with version metadata

## Versions

See [.github/versions.json](.github/versions.json) for version placeholder mappings.
# Test
# Version 1
