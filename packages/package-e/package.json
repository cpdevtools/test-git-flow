{
  "name": "@cpdevtools/test-package-e",
  "version": "0.0.0-MAIN",
  "packageManager": "pnpm@11.5.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "echo 'Building package-e...' && mkdir -p dist && echo 'export const name = \"package-e\";' > dist/index.js",
    "test": "node -e \"const pkg=require('./package.json'); if(!pkg.name) throw new Error('name missing'); if(!pkg.version) throw new Error('version missing'); console.log('\\u2713 package-e: name=' + pkg.name + ', version=' + pkg.version + ' (OK)');\"",
    "github.actions.build": "pnpm run build",
    "github.actions.test": "pnpm run test",
    "github.actions.pack": "gitflow pack"
  },
  "dependencies": {
    "@cpdevtools/test-package-d": "workspace:*"
  },
  "author": "CPDevTools",
  "license": "MIT"
}
