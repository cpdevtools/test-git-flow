name: Create Release PR

on:
  push:
    branches-ignore:
      - 'release/**'

jobs:
  create-release:
    uses: cpdevtools/git-flow/.github/workflows/create-release-pr.yml@@cpdevtools/git-flow/v0.1.0-dev.1
    with:
      branch: ${{ github.ref_name }}
      versions-file: .github/versions.yml
